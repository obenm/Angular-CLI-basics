import { ConvertToInchesPipe } from './convert-to-inches.pipe';

describe('ConvertToInchesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToInchesPipe();
    expect(pipe).toBeTruthy();
  });
});
