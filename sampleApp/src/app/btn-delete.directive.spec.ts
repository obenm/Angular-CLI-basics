import { BtnDeleteDirective } from './btn-delete.directive';

describe('BtnDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
